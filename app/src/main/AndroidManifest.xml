<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.moyang.ntoolbox">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AboutActivity"
            android:label="@string/main_menu_about"
            android:theme="@style/AppThemeActionBar">
        </activity>

        <activity
            android:name=".SearchActivity"
            android:label="@string/action_search">
        </activity>

        <activity
            android:name=".AddEventNoteActivity"
            android:label="@string/add_event_note"
            android:theme="@style/AppThemeActionBar">
        </activity>

        <activity
            android:name=".AddItemNoteActivity"
            android:label="@string/add_item_note"
            android:theme="@style/AppThemeActionBar">
        </activity>

        <activity
            android:name=".AddMoneyNoteActivity"
            android:label="@string/add_money_note"
            android:theme="@style/AppThemeActionBar">
        </activity>

        <activity
            android:name=".AddDateNoteActivity"
            android:label="@string/add_date_note"
            android:theme="@style/AppThemeActionBar">
        </activity>

    </application>

</manifest>